<UserControl x:Class="MangaSharp.Views.AppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:metro="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <UserControl.Resources>
        <Style BasedOn="{StaticResource MetroTabItem}" TargetType="{x:Type TabItem}">
            <Setter Property="metro:ControlsHelper.HeaderFontSize" Value="18"></Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="20" />
        </Grid.RowDefinitions>
        <TabControl x:Name="Items" Grid.Row="0" />
        <Grid Grid.Row="1" Background="{DynamicResource AccentColorBrush}">
            <TextBlock Text="{Binding StatusText}" Foreground="{DynamicResource WhiteBrush}" Margin="5,0,0,0" />
        </Grid>
    </Grid>
</UserControl>
